import React from "react";
import Border from "../../../components/Border/Border";

import DetailsPane from "../../Messaging/components/DetailsPane";

function ViewJob({ details }) {
  return (
    <div>
      <DetailsPane showHeader={false} />
    </div>
  );
}

export default ViewJob;
